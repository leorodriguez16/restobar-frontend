import styled from "styled-components";
import { Label } from "../Login/login.styled";
import { InputReg } from "../Register/register.style";
export const InputPassword = styled(InputReg)({
  width: "1rem",
  marginTop: "10px",
  color: "red",
});
export const LabelPassword = styled(Label)({
  margin: "10px 0px ",
});
